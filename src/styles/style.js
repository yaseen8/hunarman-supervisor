import {StyleSheet} from 'react-native';
import {colors, fonts} from '../styles';

const styles = StyleSheet.create({
  textColor: {
    color: colors.blue,
    padding: 100,
    fontSize: fonts.title,
  },
  signupButton: {
    color: colors.secondary,
    fontSize: 16,
    paddingLeft: 100,
  },
  // Other class should place here
})

export default styles;
